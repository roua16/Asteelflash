@page "/available-offers"

<PageTitle>Available Offers</PageTitle>
<div class="offers-page offers-container">
    <div class="page-header">
        <RadzenText Text="Available Offers" TextStyle="TextStyle.H3" TagName="TagName.H1" Class="page-heading" />
    </div>

    <div class="offers-wrapper">
        <div class="carousel-custom">
            <RadzenCarousel @ref="carousel" @bind-SelectedIndex="@selectedIndex" Auto="@auto" Interval="@interval"
                            PagerOverlay="false" ButtonShade="Shade.Dark" >
                <Items>
                    @foreach (var (item, i) in offers.Select((value, index) => (value, index)))
                    {
                        <RadzenCarouselItem>
                            <div class="offer-card-alt">
                                <div class="card-content">
                                    <div class="offer-header-center">
                                        <RadzenText TextStyle="TextStyle.H5" Class="offer-title">
                                            @item.Request.Title
                                        </RadzenText>
                                        <div class="price-tag">
                                            @item.Price TND
                                        </div>
                                    </div>

                                    <div class="offer-info">
                                        <div class="info-item">
                                            <div class="info-label">Delivery Date</div>
                                            <div class="info-value">@item.DeliveryDate.ToString("MMM dd")</div>
                                        </div>

                                        <div class="info-item">
                                            <div class="info-label">Supplier</div>
                                            <div class="info-value">@item.SupplierName</div>
                                        </div>
                                    </div>

                                 
                                </div>

                                <div class="card-actions">
                                    <RadzenButton Text="Accept"
                                                  ButtonStyle="ButtonStyle.Info"
                                                  ButtonType="ButtonType.Submit"
                                                  Icon="check_circle"
                                                  Shade="Shade.Darker" Variant="Variant.Filled"
                                                  Class="btn-confirm-alt"
                                                  Click="@(() => ChooseOffer(item))" />
                                </div>
                            </div>
                        </RadzenCarouselItem>
                    }
                </Items>
            </RadzenCarousel>
        </div>
    </div>
</div>




    